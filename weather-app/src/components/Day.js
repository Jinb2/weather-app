import React from 'react';
const Day = (props) => (
    <div>
        <h2>The temperature outside is {props.temp}</h2>
        <p>Weather is {props.desc} </p>
        </div>
);
        
    

export default Day;